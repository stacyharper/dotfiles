---
config:
    commands:
        update_ctags:
            - "ctags"
            - "--options=$HOME/.ctags"
            - "--recurse"
    events:
        post-checkout:
            - "update_ctags"
        post-commit:
            - "update_ctags"
        post-merge:
            - "update_ctags"
