---
config:
    commands:
        update_ctags:
            - "ctags"
            - "--options=/home/eddie/.ctags"
            - "--recurse"
    events:
        post-checkout:
            - "update_ctags"
        post-commit:
            - "update_ctags"
        post-merge:
            - "update_ctags"
