[general]
accounts = contact_personal
pythonfile = ~/.offlineimap.py
maxsyncaccounts = 1

[mbnames]
enabled = yes
filename = ~/.mutt/mailboxes
header = "mailboxes "
peritem = "+%(accountname)s/%(foldername)s"
sep = " "
footer = "\n"

[Account contact_personal]
localrepository = contact_personal-local
remoterepository = contact_personal-remote
autorefresh = 0.5
quick = 10

[Repository contact_personal-local]
type = Maildir
localfolders = ~/.mails/contact_personal

[Repository contact_personal-remote]
type = IMAP
remotehost = mail.misterbanal.net
remoteuser = contact@eddiebarraco.fr
remotepasseval = get_pass("db/Email/Contact/contact@eddiebarraco.fr")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
keepalive = 60
holdconnectionopen = yes

[Account contact_stacyharper]
localrepository = contact_stacyharper-local
remoterepository = contact_stacyharper-remote
autorefresh = 0.5
quick = 10

[Repository contact_stacyharper-local]
type = Maildir
localfolders = ~/.mails/contact_stacyharper

[Repository contact_stacyharper-remote]
type = IMAP
remotehost = mail.misterbanal.net
remoteuser = contact@stacyharper.net
remotepasseval = get_pass("db/Email/Contact/contact@stacyharper.net")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
keepalive = 60
holdconnectionopen = yes

[Account contact_misterbanal]
localrepository = contact_misterbanal-local
remoterepository = contact_misterbanal-remote
autorefresh = 0.5
quick = 10

[Repository contact_misterbanal-local]
type = Maildir
localfolders = ~/.mails/contact_misterbanal

[Repository contact_misterbanal-remote]
type = IMAP
remotehost = mail.misterbanal.net
remoteuser = contact@misterbanal.net
remotepasseval = get_pass("db/Email/Contact/contact@misterbanal.net")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
keepalive = 60
holdconnectionopen = yes

[Account ubitransport]
localrepository = ubitransport-local
remoterepository = ubitransport-remote
autorefresh = 0.5
quick = 10

[Repository ubitransport-local]
type = Maildir
localfolders = ~/.mails/ubitransport

[Repository ubitransport-remote]
type = IMAP
remotehost = imap.gmail.com
remoteuser = e.barraco@ubitransport.com
remotepasseval = get_pass("db/Ubitransport/Google")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
keepalive = 60
holdconnectionopen = yes
ssl_version = tls1_2
nametrans = lambda foldername: re.sub ('^\[Gmail\].', '',
                               re.sub (' ', '_', foldername))
folderfilter = lambda foldername: foldername not in ['[Gmail]/Tous les messages', '[Gmail]/Important']
