# An example configuration for vdirsyncer.
#
# Move it to ~/.vdirsyncer/config or ~/.config/vdirsyncer/config and edit it.
# Run `vdirsyncer --help` for CLI usage.
#
# Optional parameters are commented out.
# This file doesn't document all available parameters, see
# http://vdirsyncer.pimutils.org/ for the rest of them.

[general]
status_path = "~/.vdirsyncer/status/"

# CARDDAV
[pair personal_contacts]
a = "personal_contacts_local"
b = "personal_contacts_remote"
collections = ["from a", "from b"]
metadata = ["displayname"]

# To resolve a conflict the following values are possible:
#   `null` - abort when collisions occur (default)
#   `"a wins"` - assume a's items to be more up-to-date
#   `"b wins"` - assume b's items to be more up-to-date
#conflict_resolution = null

# CARDDAV
[storage personal_contacts_local]
type = "filesystem"
path = "~/.contacts/"
fileext = ".vcf"

[storage personal_contacts_remote]
type = "carddav"
url = "https://nextcloud.misterbanal.net/remote.php/dav"
username = "ReedWade"
password.fetch = ["command", "{{@@ home_dir_path @@}}/bin/passfirst", "db/Internet/Nextcloud", "|head -1"]

# CALDAV
[pair personal_calendar]
a = "personal_calendar_local"
b = "personal_calendar_remote"
collections = ["from a", "from b"]
metadata = ["displayname", "color"]

[storage personal_calendar_local]
type = "filesystem"
path = "~/.calendars/"
fileext = ".ics"

[storage personal_calendar_remote]
type = "caldav"
url = "https://nextcloud.misterbanal.net/remote.php/dav"
username = "ReedWade"
password.fetch = ["command", "{{@@ home_dir_path @@}}/bin/passfirst", "db/Internet/Nextcloud", "|head -1"]
